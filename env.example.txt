# Factorio Mod Manager Configuration

# Mirror URLs (primary source for mod downloads)
MIRROR_URLS=https://mods-storage.re146.dev

# Retry and timeout settings
# REQUEST_TIMEOUT increased to 120s (2 min) to handle large file downloads (59MB+)
# Connecting timeout: 10s, Read timeout: 120s
MAX_RETRIES=5
REQUEST_TIMEOUT=120

# Delay settings (in seconds) to avoid rate limiting
RANDOM_DELAY_MIN=0.5
RANDOM_DELAY_MAX=2.0

# Exponential backoff base
BACKOFF_BASE=1.4

# Concurrency settings
MAX_WORKERS=4

# Directory paths (relative to project root)
DOWNLOAD_DIR=data/downloads
IMAGES_DIR=data/images
RELEASES_DIR=data/releases
FAILED_DIR=data/failed

# Output CSV file
CSV_FILE=factorio_mods.csv

# Feature flags
SAVE_IMAGES=true
SAVE_RELEASES=false
DOWNLOAD_ZIPS=true
SAVE_CHANGELOG=false
